from sys import getsizeof
x = 12

print(getsizeof(x)) #returns memory size in bytes
#The main purpose of getsizeof() function is to get memory size in bytes occupied by an object.
#Different Python data types have different memory sizes because they store data differently.

